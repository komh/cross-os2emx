From 4e27715145e3080b2d721e0e7b8ffeaac71aa0b3 Mon Sep 17 00:00:00 2001
From: KO Myung-Hun <komh78@gmail.com>
Date: Fri, 20 Mar 2026 00:23:29 +0900
Subject: [PATCH 6/7] Fix build break

1. Add autogen.sh

2. chmod a+x contrib/relpath.sh

3. Convert EOL of emx.opt from CRLF to LF
: This fixes that options.cc is generated incorrectly due to CR

4. s/cp.exe/cp/

5. Fix incorrect merge of libiberty/pex-unix.c

6. Do not use libtool.os2

7. Do not build DLLs for libstdc++v3 and libssp
: gcc v15.2.0 does not ship those DLLs. And DLL of libstdc++-v3 cannot
be built because some symbols have too long exported name, for example,
266 chars.
---
 Makefile.in             |   5 +-
 autogen.sh              |  84 +++++++++++++++++
 contrib/relpath.sh      |   0
 gcc/config/i386/emx.opt | 203 ++++++++++++++++++++--------------------
 gcc/cp/Make-lang.in     |   2 +-
 libiberty/pex-unix.c    |   5 +-
 6 files changed, 190 insertions(+), 109 deletions(-)
 create mode 100755 autogen.sh
 mode change 100644 => 100755 contrib/relpath.sh

diff --git a/Makefile.in b/Makefile.in
index fa3d63c5ef..e39d22ef88 100644
--- a/Makefile.in
+++ b/Makefile.in
@@ -45195,8 +45195,7 @@ configure-target-libstdc++-v3:
 	  $$s/$$module_srcdir/configure \
 	  --srcdir=$${topdir}/$$module_srcdir \
 	  $(TARGET_CONFIGARGS) --build=${build_alias} --host=${target_alias} \
-	  --target=${target_alias};  \
-	  cp -p $${topdir}/libtool.os2 ./libtool \
+	  --target=${target_alias} --disable-shared  \
 	  || exit 1
 @endif target-libstdc++-v3
 
@@ -49007,7 +49006,7 @@ configure-target-libssp:
 	  $$s/$$module_srcdir/configure \
 	  --srcdir=$${topdir}/$$module_srcdir \
 	  $(TARGET_CONFIGARGS) --build=${build_alias} --host=${target_alias} \
-	  --target=${target_alias}  \
+	  --target=${target_alias} --disable-shared  \
 	  || exit 1
 @endif target-libssp
 
diff --git a/autogen.sh b/autogen.sh
new file mode 100755
index 0000000000..c394970c1c
--- /dev/null
+++ b/autogen.sh
@@ -0,0 +1,84 @@
+#!/bin/sh
+
+#
+# Regenerates all Autotools files using platform-specific GNU Autoconf
+# Automake and Libtool. This is necessary on platforms not officially
+# supported by GNU Autotools (like OS/2).
+#
+# See https://gcc.gnu.org/wiki/Regenerating_GCC_Configuration for some hints.
+#
+# Note that in general the project is not very well organized in this regard.
+# It has some contadictions in m4 macro file locations and such (in
+# particular, libtool macros live in the root directory and not in ./config
+# as all other macros) and there is no clearly defined bootstrap steps.
+# This results in some hacks we apply below in order to properly generate
+# all necessary files from scratch (they are removed from the repository to
+# avoid seeing constant differences with upstream versions because of using
+# platform-specific Autoconf tools).
+#
+
+#
+# 0. Some old scripts still need mkinstalldirs. But since it's deprecated now,
+# Automake doesn't copy it by default. Pretend we have it to make it do so.
+#
+
+touch mkinstalldirs
+
+#
+# 1. Bootstrap libssp first. It will put libtool macros and some tools to
+# an expected location (root dir) before they are needed by others.
+#
+
+echo "Bootstrapping libssp:"
+(cd libssp && autoreconf -fvi) || exit 1
+
+#
+# 2. Bootstrap Automake subprojects.
+#
+
+for f in \
+  gotools libatomic libbacktrace libcc1 libffi libgfortran libgo libgomp \
+  libhsail-rt libitm liboffloadmic liboffloadmic/plugin libphobos libquadmath \
+  libsanitizer libsanitizer libstdc++-v3 libvtv lto-plugin zlib
+do
+  test -d "$f" || { echo "Skip non-existent $f"; continue; }
+
+  echo "Bootstrapping $f:"
+  (cd $f && autoreconf -fvi) || exit 1
+done
+
+#
+# 3. Bootstrap Autoconf projects.
+#
+
+for f in \
+  fixincludes gcc intl libcpp libdecnumber libgcc libiberty \
+  libobjc
+do
+  test -d "$f" || { echo "Skip non-existent $f"; continue; }
+
+  echo "Bootstrapping $f:"
+  case "$f" in
+  libiberty)
+    # libiberty has a custom aclocal.m4, don't touch it
+    (cd $f && autoheader -f && autoconf -f) || exit 1 ;;
+  *)
+    (cd $f && aclocal -I .. -I ../config && autoheader -f && autoconf -f) || exit 1 ;;
+  esac
+done
+
+
+#
+# 4. Bootstrap Autoconf projects w/o aclocal and autoheader (including root).
+#
+
+for f in \
+  gnattools libada .
+do
+  test -d "$f" || { echo "Skip non-existent $f"; continue; }
+
+  echo "Bootstrapping $f:"
+  (cd $f && autoconf -f) || exit 1
+done
+
+echo "Done."
diff --git a/contrib/relpath.sh b/contrib/relpath.sh
old mode 100644
new mode 100755
diff --git a/gcc/config/i386/emx.opt b/gcc/config/i386/emx.opt
index 413795e581..8c38d1f4e5 100644
--- a/gcc/config/i386/emx.opt
+++ b/gcc/config/i386/emx.opt
@@ -1,102 +1,101 @@
-; EMX-specific options.
-
-; Copyright (C) 2005, 2007, 2011 Free Software Foundation, Inc.
-;
-; This file is part of GCC.
-;
-; GCC is free software; you can redistribute it and/or modify it under
-; the terms of the GNU General Public License as published by the Free
-; Software Foundation; either version 3, or (at your option) any later
-; version.
-;
-; GCC is distributed in the hope that it will be useful, but WITHOUT ANY
-; WARRANTY; without even the implied warranty of MERCHANTABILITY or
-; FITNESS FOR A PARTICULAR PURPOSE.  See the GNU General Public License
-; for more details.
-;
-; You should have received a copy of the GNU General Public License
-; along with GCC; see the file COPYING3.  If not see
-; <http://www.gnu.org/licenses/>.
-
-Zaout
-Driver
-
-Zcrtdll
-Driver
-
-Zdll
-Driver
-
-Zexe
-Driver
-
-Zhigh-mem
-Driver
-
-Zlinker
-Driver Separate Args(1)
-
-Zmap
-Driver JoinedOrMissing
-
-Zno-autoconv
-Driver
-
-Zautoconv
-Driver
-
-Zomf
-Driver
-
-Zstack
-Driver Separate Args(1)
-
-Zbin-files
-Driver
-
-Zargs-wild
-Driver
-
-Zargs-resp
-Driver
-
-Zdll-search
-Driver
-
-Zstrip
-Driver
-
-Zsym
-Driver
-
-Zno-fork
-Driver
-
-Zfork
-Driver
-
-Zunix
-Driver
-
-Zno-unix
-Driver
-
-Zsmall-conv
-Driver
-
-Zmt
-Common Ignore
-Does nothing. Preserved for backward compatibility.
-
-Zmtd
-Common Ignore
-Does nothing. Preserved for backward compatibility.
-
-pthread
-Common Ignore
-Does nothing. Preserved for backward compatibility.
-
-R
-Common Ignore
-Does nothing. Preserved for backward compatibility.
-
+; EMX-specific options.
+
+; Copyright (C) 2005, 2007, 2011 Free Software Foundation, Inc.
+;
+; This file is part of GCC.
+;
+; GCC is free software; you can redistribute it and/or modify it under
+; the terms of the GNU General Public License as published by the Free
+; Software Foundation; either version 3, or (at your option) any later
+; version.
+;
+; GCC is distributed in the hope that it will be useful, but WITHOUT ANY
+; WARRANTY; without even the implied warranty of MERCHANTABILITY or
+; FITNESS FOR A PARTICULAR PURPOSE.  See the GNU General Public License
+; for more details.
+;
+; You should have received a copy of the GNU General Public License
+; along with GCC; see the file COPYING3.  If not see
+; <http://www.gnu.org/licenses/>.
+
+Zaout
+Driver
+
+Zcrtdll
+Driver
+
+Zdll
+Driver
+
+Zexe
+Driver
+
+Zhigh-mem
+Driver
+
+Zlinker
+Driver Separate Args(1)
+
+Zmap
+Driver JoinedOrMissing
+
+Zno-autoconv
+Driver
+
+Zautoconv
+Driver
+
+Zomf
+Driver
+
+Zstack
+Driver Separate Args(1)
+
+Zbin-files
+Driver
+
+Zargs-wild
+Driver
+
+Zargs-resp
+Driver
+
+Zdll-search
+Driver
+
+Zstrip
+Driver
+
+Zsym
+Driver
+
+Zno-fork
+Driver
+
+Zfork
+Driver
+
+Zunix
+Driver
+
+Zno-unix
+Driver
+
+Zsmall-conv
+Driver
+
+Zmt
+Common Ignore
+Does nothing. Preserved for backward compatibility.
+
+Zmtd
+Common Ignore
+Does nothing. Preserved for backward compatibility.
+
+pthread
+Common Ignore
+Does nothing. Preserved for backward compatibility.
+
+R
+Common Ignore
+Does nothing. Preserved for backward compatibility.
diff --git a/gcc/cp/Make-lang.in b/gcc/cp/Make-lang.in
index 5bc9308cde..25143c820b 100644
--- a/gcc/cp/Make-lang.in
+++ b/gcc/cp/Make-lang.in
@@ -321,7 +321,7 @@ c++.install-common: installdirs
 # We can't use links because not everyone supports them.  So just copy the
 # manpage.
 doc/g++.1: doc/gcc.1
-	cp.exe $< doc/g++.1
+	cp $< doc/g++.1
 
 c++.install-man: $(DESTDIR)$(man1dir)/$(GXX_INSTALL_NAME)$(man1ext)
 
diff --git a/libiberty/pex-unix.c b/libiberty/pex-unix.c
index 6a36a4518c..eb77200667 100644
--- a/libiberty/pex-unix.c
+++ b/libiberty/pex-unix.c
@@ -287,8 +287,7 @@ pex_wait (struct pex_obj *obj, pid_t pid, int *status, struct pex_time *time)
 	    *time = pt;
 	  return cpid;
 	}
-  }
-#else
+
       if ((flags & PEX_SEARCH) != 0)
 	{
 	  execvp (executable, _argv);
@@ -299,7 +298,6 @@ pex_wait (struct pex_obj *obj, pid_t pid, int *status, struct pex_time *time)
 	  execv (executable, _argv);
 	  pex_child_error (obj, executable, "execv", errno);
 	}
-#endif
 
       psl = XNEW (struct status_list);
       psl->pid = cpid;
@@ -1005,6 +1003,7 @@ pex_unix_exec_child (struct pex_obj *obj, int flags, const char *executable,
       return pid;
     }
 }
+#endif
 #endif /* SPAWN */
 
 /* Wait for a child process to complete.  */
-- 
2.43.0

